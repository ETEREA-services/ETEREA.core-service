app:
  server: 10.147.20.25:3306
  database: tsamatriz
  user: root
  password: root
#  venta-web-server: 127.0.0.1
#  server: 10.147.17.149:3306
#  database: tsaagencia
#  user: agencia
#  password: cacheuta
  eureka: 8761
  venta-web-server: 10.147.17.149
  venta-web-port: 8280
  add-properties: true
  logging: debug
  livereload:
    enabled: true
    port: 35780
  mail:
    username: reportes.termalia@gmail.com
    password: uulgftuqhgjdihgv

server:
  port: 8080

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka:@eureka-service:${app.eureka}/eureka

spring:
  application:
    name: core-service
  datasource:
    url: jdbc:mysql://${app.server}/${app.database}?useSSL=false&serverTimezone=UTC
    username: ${app.user}
    password: ${app.password}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
  devtools:
    livereload:
      enabled: ${app.livereload.enabled}
      port: ${app.livereload.port}
    add-properties: ${app.add-properties}
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${app.mail.username}
    password: ${app.mail.password}
    properties:
      mail:
        smtp:
          starttls:
            enable: true
            required: true
          auth: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

logging:
  level:
    eterea.core.api.rest: DEBUG
    web: ${app.logging}
    org:
      hibernate:
        SQL: ${app.logging}
      springframework.cloud.config: DEBUG